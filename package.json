{
  "name": "video-card-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Simple video card builder with upload API",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check app.js && node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
